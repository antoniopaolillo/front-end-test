import { extendTheme } from '@chakra-ui/react';

const colors = {
  trybe: {
    100: '#38A791',
    200: '#4BC18D',
    300: '#256B52',
  },
};

const theme = extendTheme({ colors });

export default theme;
